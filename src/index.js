import React from 'react';
import ReactDOM from 'react-dom/client';
import RigaDadi from "./RigaDadi";

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <RigaDadi></RigaDadi>
  </React.StrictMode>
);